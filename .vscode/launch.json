{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Worklog Analytics",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/worklog_analytics/main.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}